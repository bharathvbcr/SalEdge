# Semantic layer dependencies (Python 3.10–3.14)
# One-time setup from repo root:
#   npm run semantic:setup
# Manual:
#   python3 -m venv .venv-semantic && source .venv-semantic/bin/activate
#   pip install -r requirements-semantic.txt

sentence-transformers>=3.0.0
faiss-cpu>=1.8.0
numpy>=1.26.0
pydantic>=2.0
pydantic-settings>=2.0
fastapi>=0.110.0
uvicorn>=0.27.0
httpx>=0.27.0
transformers>=4.40.0
torch>=2.0.0

# Optional alternatives
# faiss-gpu>=1.8.0          # CUDA FAISS instead of faiss-cpu
# chromadb>=0.5.0           # alternative vector store
